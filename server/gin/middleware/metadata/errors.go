package metadata

const (
	// MissingTokenError is the error for missing JWT token
	MissingTokenError = "missing token"

	// NoMetadataFoundError is the error for no metadata found
	NoMetadataFoundError = "no metadata found"
)
